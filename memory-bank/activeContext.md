# Active Context

## Current Focus

`feat/step-cache-storage-lesson-ui` — Cache Storage — отдельный раздел (`step-cache-storage-lesson-ui`)

## Current Mode

VAN complete → переход к PLAN

## Next Steps

1. `/plan` — детальный план экрана Cache Storage (структура секций, хук/мок `caches`, маршруты, TDD)
2. `/build` — реализация по плану (TDD)
3. `/reflect` — рефлексия
4. `/close-task` — финализация
5. GIT: работать только в `feat/step-cache-storage-lesson-ui`

## Context for AI

- Учебное PWA на React + Vite; менеджер пакетов — pnpm
- Level 2: VAN → PLAN → BUILD → REFLECT → CLOSE (без обязательного CREATIVE)
- Новый раздел навигации (в `lessonRoutes` пункта Cache Storage пока нет)
- Паттерн учебных экранов: data + screen (как Install / SW / Offline / Home)
- План шагов: `docs/project/implementation-plan.md`
- Дорожная карта: `docs/project/product-roadmap.md`
