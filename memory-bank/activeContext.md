# Active Context

## Current Focus

`feat/step-cache-storage-lesson-ui` — Cache Storage — отдельный раздел (`step-cache-storage-lesson-ui`)

## Current Mode

PLAN complete → переход к BUILD

## Next Steps

1. `/build` — реализация по плану (TDD: хук → data → экран → маршрут → E2E)
2. `/reflect` — рефлексия
3. `/close-task` — финализация
4. GIT: работать только в `feat/step-cache-storage-lesson-ui`

## Context for AI

- Учебное PWA на React + Vite; менеджер пакетов — pnpm
- Level 2: VAN → PLAN → BUILD → REFLECT → CLOSE (без CREATIVE)
- План в `memory-bank/tasks.md`: хук `useCacheStorage`, экран `CacheStorageScreen`, маршрут `/cache-storage`, E2E
- Паттерн: data + screen + hook (как Install / SW / Offline)
- Без тел ответов Cache API; без новых npm-зависимостей
- План шагов: `docs/project/implementation-plan.md`
- Дорожная карта: `docs/project/product-roadmap.md`
