# Active Context

## Current Focus

`chore/step-remove-lesson-stub` — удаление мёртвого компонента `LessonStubScreen`

## Current Mode

VAN COMPLETE — Level 1, следующий шаг: `/build`

## Next Steps

1. `/build` — удалить `src/components/LessonStubScreen/`, прогнать verify
2. `/reflect`
3. `/close-task`

## Context for AI

- Учебное Progressive Web App на React + Vite (только frontend)
- Менеджер пакетов: pnpm
- Зависимости задачи выполнены: home / SW / Install lesson UI завершены
- Stub нигде в `src/` не импортируется — только удаление папки
- Не трогать `.vscode/settings.json` (локальное изменение вне скоупа)
